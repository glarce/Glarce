import EventBus from './eventBus'

export default function() {
	// eslint-disable-next-line no-undef
	AFRAME.registerComponent('image-handeler', {
		schema: {
			id: {
				type: 'number'
			}
		},
		init() {
			
		}
	})
}